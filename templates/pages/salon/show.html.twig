{% extends 'base.html.twig' %}

{% block title %}Fink -
	{{salon.nom}}
{% endblock %}

{% block body %}
	<div>
		<div>
			<img src="{{asset(salon.image)}}">
			<h4>{{salon.nom}}</h4>
			<p>{{salon.ville}}</p>
			<p>{{salon.style}}</p>
			<p>{{salon.adresse}}</p>
			<p>{{salon.telephone}}</p>
			<p>{{salon.email}}</p>
		</div>
		<div>
			<p>{{salon.description}}</p>
		</div>
		<div>
			<p>Propiétaire
			</p>
			<p>{{ salon.proprietaire.nom }}
				{{ salon.proprietaire.prenom }}</p>
		</div>
		<div>
			<a href="{{ path('reservation.index',{id : salon.id})}}">Prendre rendez-vous pour discuter de votre projet</a>
		</div>
	</div>
{% endblock %}
